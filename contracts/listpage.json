{
  "name": "ListPage",
  "path": "src/patterns/ListPage/ListPage.tsx",
  "intent": "A list of records: the bar to find and filter them, the rows themselves, and the pages under them.",
  "usage": "Use ListPage for any screen that lists records: invoices, accounts, quotes. It is a blueprint: it composes kit pieces and keeps no state of its own, so the screen owns the filters, the rows and which page is showing. Pass the kit Toolbar as toolbar and the list itself as children. How the records are laid out is the Toolbar's own view menu: Table View is a kit Table, List View the same table with a two-line account cell, Card View a grid of kit Cards. The screen swaps children to match. pagination takes a kit Pagination, flush under the rows so it reads as the table's own footer, and it goes whenever something else is standing in for them. bulk is the bar of actions for the rows that are ticked; it takes the toolbar's place while something is selected, so the two are never both there. state swaps what stands in for the rows: loading draws kit Skeleton lines in their shape, empty shows what you pass as empty (a kit Empty), and error shows the error as a kit Alert. It goes inside the AppShell page, under the PageHeader, so it adds no page padding of its own. See contracts/listpage.json for the full contract.",
  "composes": [
    "Toolbar",
    "Table",
    "Card",
    "Pagination",
    "Empty",
    "Skeleton",
    "Alert"
  ],
  "props": {
    "children": {
      "type": "ReactNode",
      "required": true,
      "note": "The rows: a kit Table, or a DataGrid or a list of Cards."
    },
    "toolbar": {
      "type": "ReactNode",
      "note": "The bar over the rows: a kit Toolbar with the search, the filters, the view and the actions."
    },
    "pagination": {
      "type": "ReactNode",
      "note": "The page bar, flush under the rows as the table's footer. A kit Pagination."
    },
    "bulk": {
      "type": "ReactNode",
      "note": "What can be done to the ticked rows. While it is there it takes the toolbar's place."
    },
    "state": {
      "enum": [
        "ready",
        "loading",
        "empty",
        "error"
      ],
      "default": "ready",
      "note": "What the list is doing. ready shows the rows; the others stand in for them."
    },
    "empty": {
      "type": "ReactNode",
      "note": "Shown when state is empty, usually a kit Empty with the action that starts the first record."
    },
    "error": {
      "type": "string",
      "note": "Shown when state is error, as a kit Alert. Say what failed and what to do."
    },
    "onRetry": {
      "type": "() => void",
      "note": "Adds Try again to that Alert."
    },
    "loadingRows": {
      "type": "number",
      "default": 6,
      "note": "How many Skeleton lines stand in while it loads. Match the page size so the page keeps its height."
    },
    "label": {
      "type": "string",
      "note": "Names the list for screen readers, like Invoices. Without it, the region is named List."
    }
  },
  "useWhen": [
    "A screen that lists records with a search, filters and pages.",
    "A list that has to show loading, empty and failed states.",
    "A list people can lay out more than one way."
  ],
  "doNot": [
    "Do not add state to it; it is a blueprint. Keep the filters, the rows, the selection and the page in the screen.",
    "Do not build the bar, the rows or the pages inside it. Pass the kit pieces.",
    "Do not show the bulk bar and the toolbar at once; passing bulk swaps them.",
    "Do not add page padding around it. It sits in the AppShell page, which owns that.",
    "Do not use it for one record's details. That is a record screen.",
    "Do not use raw hex.",
    "Do not give the table a caption that repeats label; the list is already named."
  ],
  "a11y": [
    "The list is a region named after label, so a screen reader can jump to it.",
    "While it loads the region is marked busy, so a change is not announced as new rows.",
    "The empty and failed states replace the rows in the same place, so focus stays where the rows were.",
    "The bulk bar says how many rows are ticked."
  ],
  "snippet": "<ListPage label=\"Invoices\" toolbar={<Toolbar />} pagination={<Pagination total={248} />}><Table columns={columns} rows={rows} /></ListPage>",
  "examples": [
    {
      "title": "A list",
      "usage": "The bar, the rows and the pages under them.",
      "items": [
        {
          "props": {
            "state": "ready"
          }
        }
      ]
    },
    {
      "title": "Nothing yet",
      "usage": "Before the first record exists.",
      "items": [
        {
          "props": {
            "state": "empty"
          }
        }
      ]
    }
  ]
}
