{
  "name": "LineChart",
  "path": "src/ui/LineChart/LineChart.tsx",
  "intent": "How values change over time as lines or filled areas, one per series, like revenue by month or invoices approved by day.",
  "usage": "Use LineChart for trends across ordered categories, usually time. It is one component: pass categories (the labels along the bottom) and series (one values array each). area fills under each line; stacked piles the areas on each other so the top edge is the total, like invoices by severity. showPoints marks each value with a dot and showValues writes it above. Hover or use the arrow keys for a tooltip with every series at that point. animate plays a subtle draw-in. Put the chart's title and report link in the Section or Card around it. ChartTone: green | olive | cyan | orange | pink | gray | purple | yellow | red | mint (the kit category colors). title and subtitle write a heading over the chart; showTitle turns it off when the Card around it already says the same. The keys under the chart are the kit Legend; legend moves them over it or beside it. See contracts/linechart.json for the full contract. A legend beside the chart drops under it when the chart's own box is narrower than 360px.",
  "props": {
    "label": {
      "type": "string",
      "required": true,
      "note": "Names the chart for screen readers and captions its data table, like Revenue by period."
    },
    "title": {
      "type": "string",
      "note": "Visible title over the chart, like Revenue by period. The screen-reader name still comes from label."
    },
    "subtitle": {
      "type": "string",
      "note": "A quieter line under the title, like Last 12 months, all accounts."
    },
    "showTitle": {
      "type": "boolean",
      "default": true,
      "note": "Shows the title and subtitle. Off draws the chart alone, when the Card or Section around it already carries the title."
    },
    "categories": {
      "type": "string[]",
      "required": true,
      "note": "The labels along the bottom, like Jan to Jun. Labels thin out when they would overlap."
    },
    "series": {
      "type": "ChartSeries[]",
      "required": true,
      "itemShape": "{ name: string; values: number[]; tone?: ChartTone }",
      "note": "One entry per line or bar color, with one value per category. tone picks the color; by default series take green, olive, cyan, orange, pink, gray, then purple, yellow, red and mint."
    },
    "area": {
      "type": "boolean",
      "default": false,
      "note": "Fills under each line. Overlapping areas are see-through."
    },
    "stacked": {
      "type": "boolean",
      "default": false,
      "note": "Piles each series on the ones before it, as filled areas; the top edge is the total."
    },
    "showPoints": {
      "type": "boolean",
      "default": false,
      "note": "A white dot with a colored ring at each value."
    },
    "showValues": {
      "type": "boolean",
      "default": false,
      "note": "The short value above each point. For a few points only."
    },
    "showGrid": {
      "type": "boolean",
      "default": true,
      "note": "Faint lines at each value tick."
    },
    "showLegend": {
      "type": "boolean",
      "default": true,
      "note": "Colored squares with the series names under the chart."
    },
    "legend": {
      "enum": [
        "top",
        "bottom",
        "end"
      ],
      "default": "bottom",
      "note": "Where the keys sit: under the chart, over it, or in a column beside it."
    },
    "animate": {
      "type": "boolean",
      "default": true,
      "note": "Subtle motion when the chart appears. Off draws it at once. Always off when the person prefers reduced motion."
    },
    "format": {
      "enum": [
        "number",
        "currency",
        "percent"
      ],
      "default": "number",
      "note": "How values read. Axis ticks and value labels are short ($4.2M, 750K); the tooltip and table show the full number."
    },
    "currency": {
      "type": "string",
      "default": "USD",
      "note": "ISO code for format currency."
    },
    "height": {
      "type": "number",
      "default": 240,
      "note": "Height of the drawing in px, axis labels included. The width fills the container."
    },
    "emptyLabel": {
      "type": "string",
      "default": "No data for this range.",
      "note": "Shown when there are no categories or series."
    }
  },
  "useWhen": [
    "Trends over time: revenue by month, approvals by day, a burndown.",
    "Part-to-whole over time with stacked areas."
  ],
  "doNot": [
    "Do not use it for categories without an order, like regions. Use BarChart.",
    "Do not use it for one share of a total. Use PieChart.",
    "Do not draw charts with loose SVG or a chart library. Use the kit charts.",
    "Do not use raw hex; pick a tone."
  ],
  "a11y": [
    "A figure named by label. The drawing is hidden from screen readers; a visually hidden table holds every value, with the categories as rows and the series as columns.",
    "The chart takes focus: the arrow keys (and Home and End) move through the categories and show the same tooltip as pointing at them. Escape hides it.",
    "Colors come with a legend and the tooltip names each series, so color is not the only way to tell them apart.",
    "Motion is subtle and optional (animate), and never plays when the person prefers reduced motion."
  ],
  "snippet": "<LineChart\n  label=\"Revenue by period\"\n  categories={[\"Jan\", \"Feb\", \"Mar\", \"Apr\", \"May\", \"Jun\"]}\n  series={[{ name: \"Revenue\", values: [3.4e6, 3.6e6, 4.2e6, 4.4e6, 4.6e6, 4.9e6] }]}\n  format=\"currency\"\n  area\n  showPoints\n  showValues\n/>",
  "examples": [
    {
      "title": "Lines",
      "usage": "Two series as lines. Hover for both values on the same day.",
      "items": [
        {
          "props": {
            "label": "Invoices approved by time",
            "categories": "{days}",
            "series": "{approvals}"
          }
        }
      ]
    },
    {
      "title": "Area with values",
      "usage": "One filled series with its points and values, like revenue by period.",
      "items": [
        {
          "props": {
            "label": "Revenue by period",
            "categories": "{months}",
            "series": "{revenue}",
            "format": "currency",
            "area": true,
            "showPoints": true,
            "showValues": true
          }
        }
      ]
    },
    {
      "title": "Stacked areas",
      "usage": "Series pile on each other; the top edge is the total. Points mark each value.",
      "items": [
        {
          "props": {
            "label": "Invoices by severity",
            "categories": "{severityDays}",
            "series": "{severity}",
            "format": "currency",
            "stacked": true,
            "showPoints": true
          }
        }
      ]
    }
  ]
}
