{
  "name": "BarChart",
  "path": "src/ui/BarChart/BarChart.tsx",
  "intent": "Values side by side as bars, one group or stack per category, like invoices by status for each week. Bars stand up, or run across with orientation horizontal.",
  "usage": "Use BarChart to compare amounts across categories or periods. It is one component: pass categories and series. orientation vertical puts the categories along the bottom with bars standing up; horizontal lists them down the start with bars running across, best for long names or ranked lists like top customers. Bars sit side by side per category, or on top of each other with stacked, so the bar height is the total and the colors are its parts. barWidth thin draws 8px bars with rounded ends, for a long run of periods like a month of daily use. showValues writes each value at the end of its bar. Hover or use the arrow keys for a tooltip with every series in that category. animate lets bars grow out from the baseline. Put the chart's title and report link in the Section or Card around it. ChartIntent: green | olive | cyan | orange | pink | gray | purple | yellow | red | mint (the kit category colors). title and subtitle write a heading over the chart; showTitle turns it off when the Card around it already says the same. highlight picks out one or more bars in the highlight intent, like the biggest and smallest in a ranked list. The keys under the chart are the kit Legend; legend moves them over it or beside it. See contracts/barchart.json for the full contract. A legend beside the chart drops under it when the chart's own box is narrower than 360px.",
  "props": {
    "label": {
      "type": "string",
      "required": true,
      "note": "Names the chart for screen readers and captions its data table, like Revenue by period."
    },
    "title": {
      "type": "string",
      "note": "Visible title over the chart, like Revenue by period. The screen-reader name still comes from label."
    },
    "subtitle": {
      "type": "string",
      "note": "A quieter line under the title, like Last 12 months, all accounts."
    },
    "showTitle": {
      "type": "boolean",
      "default": true,
      "note": "Shows the title and subtitle. Off draws the chart alone, when the Card or Section around it already carries the title."
    },
    "categories": {
      "type": "string[]",
      "required": true,
      "note": "The category labels: along the bottom when vertical (they thin out when they would overlap), down the start when horizontal (long ones shorten with an ellipsis; the tooltip shows the full name)."
    },
    "series": {
      "type": "ChartSeries[]",
      "required": true,
      "itemShape": "{ name: string; values: number[]; intent?: ChartIntent }",
      "note": "One entry per line or bar color, with one value per category. intent picks the color; by default series take green, olive, cyan, orange, pink, gray, then purple, yellow, red and mint."
    },
    "orientation": {
      "enum": [
        "vertical",
        "horizontal"
      ],
      "default": "vertical",
      "note": "vertical: categories along the bottom, bars standing up. horizontal: categories down the start, bars running across, values along the bottom."
    },
    "highlight": {
      "type": "number | number[]",
      "note": "Category numbers to pick out, counted from 0, like the top and bottom of a ranked chart. They take the highlight intent instead of their series color."
    },
    "highlightIntent": {
      "enum": [
        "green",
        "olive",
        "cyan",
        "orange",
        "pink",
        "gray",
        "purple",
        "yellow",
        "red",
        "mint"
      ],
      "default": "orange",
      "note": "The color for picked-out bars."
    },
    "stacked": {
      "type": "boolean",
      "default": false,
      "note": "Piles the series in one bar per category; the bar height is the total."
    },
    "barWidth": {
      "enum": [
        "wide",
        "thin"
      ],
      "default": "wide",
      "note": "wide: bars fill most of their category's space, with square corners. thin: every bar is 8px, like a medium Progress bar, with its rounded ends; a stack rounds only its two outer ends. For long runs of periods, like a month of days. Pointing anywhere in a category still shows its tooltip."
    },
    "showValues": {
      "type": "boolean",
      "default": false,
      "note": "The short value at the end of each bar (side-by-side bars) or each stack: above it when vertical, past it when horizontal."
    },
    "showGrid": {
      "type": "boolean",
      "default": true,
      "note": "Faint lines at each value tick."
    },
    "showLegend": {
      "type": "boolean",
      "default": true,
      "note": "Colored squares with the series names under the chart."
    },
    "legend": {
      "enum": [
        "top",
        "bottom",
        "end"
      ],
      "default": "bottom",
      "note": "Where the keys sit: under the chart, over it, or in a column beside it."
    },
    "animate": {
      "type": "boolean",
      "default": true,
      "note": "Subtle motion when the chart appears. Off draws it at once. Always off when the person prefers reduced motion."
    },
    "format": {
      "enum": [
        "number",
        "currency",
        "percent"
      ],
      "default": "number",
      "note": "How values read. Axis ticks and value labels are short ($4.2M, 750K); the tooltip and table show the full number."
    },
    "currency": {
      "type": "string",
      "default": "USD",
      "note": "ISO code for format currency."
    },
    "height": {
      "type": "number",
      "note": "Height of the drawing in px, axis labels included. Vertical defaults to 240. Horizontal grows by 36 a category unless you set it. The width fills the container."
    },
    "emptyLabel": {
      "type": "string",
      "default": "No data for this range.",
      "note": "Shown when there are no categories or series."
    }
  },
  "useWhen": [
    "Comparing amounts across categories or periods.",
    "Parts of a total per period, with stacked.",
    "Long category names or a ranked list, like top customers by balance, with orientation horizontal.",
    "A ranked list where one or two bars matter most: sort the data and pick them out with highlight.",
    "A long run of periods, like usage per day for a month, with barWidth thin so the bars read as a light rhythm."
  ],
  "doNot": [
    "Do not use it for a long, smooth trend. Use LineChart.",
    "Do not use it for one share of a total. Use PieChart.",
    "Do not draw charts with loose SVG or a chart library. Use the kit charts.",
    "Do not use raw hex; pick an intent.",
    "Do not use highlight on every bar; pick out one or two, or use a series intent.",
    "Do not use barWidth thin for a handful of categories; the bars get lost in wide bands. Keep wide."
  ],
  "a11y": [
    "A figure named by label. The drawing is hidden from screen readers; a visually hidden table holds every value, with the categories as rows and the series as columns.",
    "The chart takes focus: the arrow keys (and Home and End) move through the categories, in either orientation, and show the same tooltip as pointing at them. Escape hides it.",
    "Colors come with a legend and the tooltip names each series, so color is not the only way to tell them apart.",
    "Motion is subtle and optional (animate), and never plays when the person prefers reduced motion."
  ],
  "snippet": "<BarChart\n  label=\"Invoices by status by time\"\n  categories={weeks}\n  series={[\n    { name: \"Current\", values: current, intent: \"pink\" },\n    { name: \"Paid\", values: paid, intent: \"green\" },\n  ]}\n  format=\"currency\"\n  stacked\n/>",
  "examples": [
    {
      "title": "Stacked",
      "usage": "One bar per week; its colors are the invoice statuses and its height the total.",
      "items": [
        {
          "props": {
            "label": "Invoices by status by time",
            "categories": "{weeks}",
            "series": "{statuses}",
            "format": "currency",
            "stacked": true
          }
        }
      ]
    },
    {
      "title": "Side by side",
      "usage": "Series next to each other in each category, with their values.",
      "items": [
        {
          "props": {
            "label": "Revenue by type",
            "categories": "{months}",
            "series": "{revenueByType}",
            "format": "currency",
            "showValues": true
          }
        }
      ]
    },
    {
      "title": "Horizontal",
      "usage": "Categories down the start and bars running across, for long names or a ranked list.",
      "items": [
        {
          "props": {
            "label": "Open balance by customer",
            "categories": "{customers}",
            "series": "{balances}",
            "format": "currency",
            "orientation": "horizontal",
            "showValues": true
          }
        }
      ]
    },
    {
      "title": "Thin bars",
      "usage": "A month of days, stacked by balance, as 8px bars with rounded ends.",
      "items": [
        {
          "props": {
            "label": "Credits used per day by balance",
            "categories": "{monthDays}",
            "series": "{creditsByBalance}",
            "stacked": true,
            "barWidth": "thin"
          }
        }
      ]
    }
  ]
}
